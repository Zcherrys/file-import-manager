{
  "name": "file-import-manager",
  "version": "1.0.0",
  "description": "Obsidian plugin to manage file imports based on file types",
  "main": "main.js",
  "scripts": {
    "dev": "tsc -watch",
    "build": "tsc && esbuild dist/main.js --bundle --platform=node --format=cjs --external:obsidian --outfile=dist/bundled.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "file-import"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "tslib": "^2.3.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "esbuild": "^0.19.3",
    "eslint": "^8.1.0",
    "obsidian": "^0.15.0",
    "tslint": "^6.1.3",
    "typescript": "^4.4.4"
  }
}